import { createTheme } from "@mui/material/styles";

const Theme = createTheme({
  palette: {
    primary: {
      main: "",
    },
    secondary: {
      main: "",
    },
    text: {
      primary: "",
      secondary: "",
    },
  },
});

export default Theme;
